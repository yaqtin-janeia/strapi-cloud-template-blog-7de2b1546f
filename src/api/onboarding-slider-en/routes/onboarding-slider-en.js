'use strict';

/**
 * onboarding-slider-en router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::onboarding-slider-en.onboarding-slider-en');
