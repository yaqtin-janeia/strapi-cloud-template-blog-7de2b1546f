'use strict';

/**
 * onboarding-slider controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::onboarding-slider.onboarding-slider');
