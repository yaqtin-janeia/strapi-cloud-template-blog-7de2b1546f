'use strict';

/**
 * onboarding-slider service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::onboarding-slider.onboarding-slider');
